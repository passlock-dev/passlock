{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    /* Projects */
    "incremental": true,
    "composite": true,

    /* Output */
    "rootDir": "./src",
    "outDir": "./dist",
    "stripInternal": true,

    // library-friendly defaults (adjust to your setup)
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,

    // usually you don't want tests or dev-only types leaking into build
    "types": ["node"]
  },
  "include": ["src/**/*"],
  "exclude": [
    "**/*.test.ts",
    "**/*.spec.ts",
    "test/**/*",
    "dist",
    "node_modules"
  ]
}
